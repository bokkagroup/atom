<div class="organism slider-gallery" id="{{id}}">
    <section>
        <div class="container">
            {{> molecules/text}}
            <div class="slider">
                <ul class="slides">
                    {{#each gallery}}
                        {{> molecules/slide}}
                    {{/each}}
                </ul>
            </div>
        </div><!--/container -->
    </section>
</div><!--/slider-gallery-->